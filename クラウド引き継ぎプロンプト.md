# クラウド版 Claude Code への引き継ぎプロンプト

使い方：下の「---」から最後までを、クラウドのCodeの最初のメッセージに貼り付ける。
前提：アプリの元ファイル（app.html など）が GitHub のリポジトリに入っていること（入っていないとクラウド側で修正できない）。

---

このリポジトリ（shinagawashrine-bit/mahjong）は、仲間内の麻雀の成績を記録する1ファイルのWebアプリです。GitHub Pages で https://shinagawashrine-bit.github.io/mahjong/ に公開しています。これまでMacのローカル環境で開発してきましたが、今日からクラウドで続きをやります。まず状況を把握してください。

## 最初にやること（作業に入る前に）

1. `引き継ぎメモ.md` を全部読む。設計で決めたこと・直したバグ・共有の仕組みが書いてある。「設計で決めたこと」を変えるときは、変える理由を私に確認すること
2. `git log --oneline -15` を見る。**メモは 2026/9/24 16:39 時点のもの**で、そのあと 9/25 に5つのコミット（55f15d6〜4c66bd0：自分の成績表示、画面の見出し、閲覧者の表示設定、使い方ガイドの二章構成化など）が入っている。この5つの中身は `git show` で確かめ、メモに書かれていない変更として把握する
3. 下の「ファイルの構成」にあるファイルが実際にあるか確かめる。足りなければ作業を始めずに私に伝える
4. 把握した内容を短くまとめて報告し、私の指示を待つ

## ファイルの構成

- `app.html` … **アプリの本体（ここを直す）**。head/body タグを持たない断片
- `index.html` … `build.sh` が app.html から作る。**直接編集しない**
- `build.sh` … index.html を作り、使い方ガイドを `guide/` にコピーする
- `deploy.sh` … build → index.html・アイコン・guide/ をコミットして main に push（＝公開）
- `guide/` … 公開中の使い方ガイド。元は `マニュアル/20260925_mahjong-guide-web.html` と `マニュアル/images/`
- `マニュアル/` … ガイドの元ファイル。PDF版もここ。**`使い方ガイド.html` は古い版**なので使わない
- `20260925_ui-regression.cjs` … Node の vm で app.html のロジックを試すテスト（`node 20260925_ui-regression.cjs`）
- `tools/screenshots/` … ガイド用スクショの撮影道具（偽の保管庫サーバー `server.py`、撮影スクリプト `shot.sh`、操作 `shot-actions.js`、PDF→画像 `pdf2png.swift`）。**Mac 用に書いてある**（Chrome のパス、swift）。クラウドで使うなら Linux 向けに直す
- `共有スクリプト.gs` … 保管庫（Googleスプレッドシート）側の Apps Script。触らない
- `引き継ぎメモ.md`、`レビュー依頼プロンプト.md`、`マニュアル作成依頼プロンプト.md` … 資料

## 仕組みの要点（詳しくはメモ）

- データは各端末の localStorage が本体。共有は Google Apps Script の保管庫に JSON を預ける
- リンクは2種類：`?s=合言葉`（閲覧者）、`?e=合言葉`（編集者＝入力できる）
- 共有は**突き合わせ（マージ）方式**：日・半荘・メンバーごとに時刻（`s.at` `g.at` `m.at` `c.metaAt`）を持ち、新しい方を残す。消したものは `c.tomb`。時刻は `save()` → `stamp()` が差分から自動で付ける。読み込んだあとは `SNAP = null; save()`（読んだものに時刻を付けない。忘れると送り合いが止まらなくなる）
- 送れたかは `bodySig` と `c.sentSig` の比較。その日の画面の右上に「送信済み ✓／未送信・今すぐ送る」
- 画面は **iPhone の見た目が基準**。広い画面向けは `@media (min-width:760px)` と `(min-width:1100px)` の中だけに書く

## 守ってほしいこと

- **本物の保管庫（`DEFAULT_SCRIPT_ID` の Google Apps Script）に向けてテストしない。** 私のGoogleドライブにデータが書き込まれる。共有まわりを試すときは `tools/screenshots/server.py` のような偽の保管庫をローカルに立て、app のURLをそこに向けたテスト用コピーで試す（前回はそうした）
- **公開は、私が「公開して」と言ったときだけ。** 公開＝main への反映（GitHub Pages が main から配信）。クラウドから main に直接 push できるか、ブランチ＋プルリクエストにするかは、最初の公開の前に私に確認すること。`deploy.sh` はMac用の専用鍵（`~/.ssh/id_ed25519_mahjong`）を前提にしているので、クラウドではそのままでは動かない可能性がある
- 公開したら、公開ページを取得して反映されたことを確かめてから報告する
- 保存しているデータの形を変えるとき（共有・バックアップ・localStorage）は、既存の記録と仲間の端末への影響を先に説明する
- 直したら `node 20260925_ui-regression.cjs` を通す。画面の変更はブラウザで実際に動かして確かめる
- 画面を変えたら、使い方ガイド（`マニュアル/20260925_mahjong-guide-web.html` と PDF）の該当箇所も直す必要があるか確認する
- `引き継ぎメモ.md` を最新に保つ。設計の決定・直したバグを書き足す

## 私とのやりとりのルール

- 日本語で。プロフェッショナルだが会話的に。説明は自然な段落で、リストは箇条書き
- 複雑な作業を始める前に、明確化の質問をする
- 「確認できたこと／推測／わからないこと」を分けて答える。推測で埋めない
- 安易に同意しない。前提の甘さ・リスク・矛盾があればはっきり指摘する。前提を変えた方がよい案があれば出す
- 私の指示を設計に反映するときは、「指示にあったこと」と「自分が付け足した・解釈したこと」を分けて示す
- 確認の報告は事実どおりに。試していないことは「試していない」と言う
