# 麻雀成績記録アプリ ― 引き継ぎメモ

最終更新: 2026年9月24日

## URL

- **アプリ本体** https://shinagawashrine-bit.github.io/mahjong/
- **保管庫（アプリに1回貼るだけ。仲間には渡さない）**
  `https://script.google.com/macros/s/AKfycbyx4wvBOwWha7YM2xLRwyCEILSU1Ktx9iGJH0OfEaD7oCFNClRh_9JPgWGL6AFJcnAp/exec`
- **データの実体** Googleドライブの「麻雀成績データ」スプレッドシート（1コミュニティ＝1行）
- **GitHub** shinagawashrine-bit/mahjong（Public）

claude.ai のアーティファクト版はもう使わない（記録が分かれるため）。

## 残っている作業（本人の操作）

1. JYU-KEN → 設定 → 共有をはじめる → 合言葉 **品川神社** と入力（長いURLでも可）
2. 出てきたリンクを仲間に送る（閲覧者用と編集者用の2本が出る）

**合言葉「品川神社」は仲間に教えない。**教えると相手のデータが本人のドライブに入る。
設定リンク `…/mahjong/?setup=品川神社` を1回開く方法もあるが、
**ホーム画面のアプリとSafariは保存先が別**なので、使う方（ホーム画面のアプリ）で設定すること。

## 更新のしかた（Claude側）

`app.html` を直して `./deploy.sh "変更内容"` を実行すると公開ページに反映される。
`index.html` は `build.sh` の自動生成なので直接編集しない。
アイコンは `icon-src.png`（1254px）が元。差し替えたら PIL で 180px（apple-touch-icon.png）と
32px（favicon.png）を作り直し、`build.sh` の `?v=` を上げる。deploy.sh がこの2枚も一緒に送る。
**ホーム画面のアイコンは追加した時点で焼き付く**ので、変えたら本人が一度削除して追加し直す必要がある。
認証はこのフォルダ専用のデプロイキー（`~/.ssh/id_ed25519_mahjong`）。会社のGitHubアカウントには触れない。

## 共有の仕組み

- 入力する人の端末が本体。記録すると約1秒後にスプレッドシートへ自動送信
- リンクは2種類。`?s=合言葉` が閲覧者、`?e=合言葉` が編集者
- 保管庫IDはアプリ内の `DEFAULT_SCRIPT_ID` に持たせてリンクを短縮（59文字）。長い形 `?s=ID.合言葉[.e]` も読める
- 閲覧者は編集できない。設定タブも半荘追加も出ない
- 編集者は入力できる。2026/9/24から**突き合わせ（マージ）方式**：日・半荘・メンバーごとに新しい方を残すので、2人同時に入力しても両方残る
  （同じ半荘を2人が入れると2件になる。そのときは片方を削除）
  - 時刻は `save()` → `stamp()` が前回の控え（`SNAP`）との差分から自動で付ける：`s.at` `g.at` `m.at` `c.metaAt`
  - 消したものは `c.tomb` に `s:日 / g:半荘 / m:メンバー` → 時刻。日のIDは印（`shared_合言葉_`）を外した形で持つ
  - 半荘は `g.id`（古い記録は `日ID_L番号` で端末が違っても同じになる）と `g.c`（並び順）を持つ。修正・削除はIDで探す
  - 送る前に必ず読んで突き合わせる（`pushShare`→`pullShare`→`mergeShared`→`sendShare`）。閲覧者は `replaceShared` で写すだけ
  - 送れたかは「指紋」（`bodySig`、キー順とIDの印に左右されない）と `c.sentSig` の比較。その日の画面の右上に「送信済み ✓／未送信・今すぐ送る」
  - 読み込んだあとは `SNAP = null; save()` にする（読んだものに自分の時刻を付けない）。これを忘れると送り合いが止まらなくなる
  - `guestOf` は印を外した日のIDで持つ（送り主と受け取り側で食い違わないように）
- 受け取り側は、開いたとき・戻ったとき・1分ごとに自動で読み直す。手動の「今すぐ読み込む」は閲覧者だけに表示
- 仲間が自分のコミュニティを共有しようとしても、保管庫URLがないので始められない。他人のデータが本人のドライブに入ることはない
- 共有のボタンは「データを保存」（今すぐ送信）と「データを更新」（保管庫の方が新しければ取り込む）
- 送り主が読み直すのは**起動時と、他アプリから戻ったとき**のみ（1分おきの自動読み込みは閲覧者だけ）
- 自動バックアップ：ボタンは4つ。**自動でバックアップ**（初回は有効化、以後は今すぐ控える）／**自動で復元**（合言葉が無い端末では合言葉を聞いてから読みにいく＝機種変え対応）／**手動でバックアップ**（文字列で書き出し）／**手動で復元**（文字列から戻す）。保管庫の行の合言葉は bk_ 始まり。合言葉はトップに表示される
- 保管庫の指定は `resolveVault()` に集約。**合言葉「品川神社」（VAULT_CODE）** か `/exec` で終わるURLのどちらでも通る。`?setup=品川神社` でも可。
  読み（しながわじんじゃ／シナガワジンジャ／shinagawa）も VAULT_ALIASES で受ける。NFKC正規化＋空白除去済み
- 接続状態は設定の共有欄とトップのバックアップ欄に「保管庫 接続済み／未接続」と出る
- アプリに保管庫IDを既定で効かせないのは、仲間が自分のコミュニティを共有したときに本人のドライブへ入るのを防ぐため。合言葉はソースを読めば分かるので秘密ではなく、うっかり接続の防止
- **ホーム画面に追加したアプリとSafariは localStorage が別**。片方で設定しても、もう片方には反映されない

## 手を付けていない改善

- **年別・期間別の集計** ― 今は全期間の通算のみ。外部レビューで「振り返り指標の最大の穴」と指摘された
- **セッションのメモ欄** ― 役満の記念など
- **超大画面の余白** ― 1100px以上でも列は1000pxで止める。左右の余りは使っていない
  （表や文章を横に伸ばすと逆に読みにくいため。一覧を常時出す2画面構成にするなら要検討）
- **共有の準備手順をアプリ内に案内** ― 仲間から頼まれたら実装する

## 実戦で確かめること

- 卓の横で入力が間に合うか（**まだ一度も実戦投入していない**）
- 数日空けても記録が残っているか（ホーム画面に追加した状態で）
- 共有リンクが仲間の端末で正しく開くか

## 仲間向けマニュアル

`マニュアル/麻雀成績アプリ_使い方ガイド.pdf`（元は同じフォルダの `使い方ガイド.html` と `images/`）。
Chrome の headless で HTML から PDF にしている。画面を変えたらスクショを撮り直すこと（見本データは架空の「金曜麻雀会」）

## 設計で決めたこと（変更するときは理由を確認）

- 卓を始めたら「今日のメンバー」のシートを出す。その日の画面の上に「今日のメンバー」「ルール」の行と「変更」ボタン
- その日だけのルールは、その日の画面の「ルール → 変更」（`sheet.kind="dayRules"`、`s.rules` を直接変える。人数は変えない）。
  設定タブのルールは次の卓からの既定。**編集者（入力用リンク）もルールを変えられる**（本人の指示。日ごとにルールが変わるため）
- 使い方ガイドは `build.sh` が `マニュアル/` から `guide/` にコピーして一緒に公開する。アプリのトップと共有の帯からリンク

- 半荘は終局時の持ち点を100点単位で入力（欄に「00」を固定表示）。着順・ウマ・オカ・五捨六入は自動。合計検算あり
- 3人ぶん入れると「残りを入れる」で4人目が確定
- 五捨六入は持ち点を千点単位に丸めるのと同じ扱い（29,500点 → 素点−1）
- チップは終了時の枚数、祝儀・ペナルティはポイント。符号は±ボタン（iPhoneにマイナスキーがないため）
- 成績とチップはポイント、収支は円。（成績＋チップ）×レート＝収支
- ルールは卓を始めた時点のものを日ごとに保存。過去は変わらない。遡及は設定のボタンから
- 着順の色は 1着=金 / 2着=銀 / 3着=銅 / 4着=紺
- ゲストはその日だけ。通算には入らない
- 引き継ぎは収支だけに効かせる（着順率には影響させない）
- 削除は9秒間取り消せる
- 配色はライト固定（端末の夜モードに追随しない）
- 局ごとの記録（和了率・放銃率）は入力負担が大きすぎるので採らない
- 収支に「1半荘あたり」は出さない。引き継ぎがある人だけ「うち引き継ぎ ¥○○」を添える
- 折れ線グラフは0を起点にせず、最初の記録から描く
- 飛び賞（2026/9/24追加）：設定のルールで「あり／なし」とポイント数（既定10）。持ち点が**マイナス**の人が飛び（0点ちょうどは飛ばない。成績の飛び回数と同じ基準）。
  半荘の入力で飛ばした人を選ばせ、選ぶまで記録できない。`g.tobiBy = {飛んだ人: 飛ばした人}` で持つ。
  選択肢に「手動」があり、選ぶとポイントは自動で動かない（ダブロンなどは精算タブで手入力。値は `"manual"`）
  ポイントは**成績ではなく祝儀・ペナルティに加算**（本人の選択）。精算タブの手入力欄とは別に「飛び賞（自動）」として表示し、手入力の値には混ぜない

### 画面の幅について（Mac・iPad対応）

- **iPhoneの見た目が基準。狭い画面の指定は一切変えない。**広い画面向けはすべて
  `@media (min-width:760px)` と `(min-width:1100px)` の中だけに書く
- 列の幅は 520px（〜759）→ 760px（760〜1099）→ 1000px（1100〜）
- カードを2列に並べたい所は `<div class="cards">` で包む。この包みは狭い画面では
  `display:contents` になり、レイアウト上「無いもの」として扱われる（従来の並びが崩れない）
- 折れ線グラフだけはCSSで伸ばせない。枠に対して viewBox が小さいと軸の文字まで拡大され、
  大きいと潰れて読めなくなるため、`lineChart` の最後の引数で形を指定する。
  全幅に置く成績タブのグラフ＝横長（760×210）／半分幅に置くその日のグラフ＝従来（360×150）
- 幅が境目をまたいだらグラフを描き直す。`matchMedia` の change と resize の二重がけ。
  入力中とシートを開いている間は描き直さない
- 設定タブは入力欄なので `main.narrow` で600pxに抑える。横に伸ばさない
- 入力シートは広い画面では画面中央のダイアログになる。出現の動きに transform を使っているので、
  中央寄せは transform ではなく `inset:0 + margin:auto + height:fit-content` で行う
- 広い画面では body を少し暗く（#D9D9CE）してアプリの列を「紙」に見せる

## 直したバグ（再発させないこと）

- 編集者の端末から記録が送られていなかった（自動送信が `DB.syncId`＝保管庫接続の有無で止まっていた。編集者は保管庫につながないので必ず止まる）。2026/9/24修正：送り先は各コミュニティの `c.share` を見る
- 入力用リンクを開き直すと、送れていなかった記録が保管庫の中身で消えていた（`applyShared` が丸ごと置き換えていた）。2026/9/24にマージ方式にして解決
- 友達が追加したゲストが、送り主の端末でその日の参加者に出なかった（`guestOf` の日IDの印の有無）。同日解決

- 五捨六入がマイナス側だけ逆方向に丸まっていた
- ルール欄を空にすると `Number("")` が 0 になり、レート0などが入った
- 一度も打っていないメンバーが「記録に使われている」判定で消せなかった
- ホームの半荘数がゲスト同卓分を数え落としていた
- 共有の送信要否の判定に時刻を含めており、毎回送信していた
- 符号ボタンを `<label>` の中に置いたため、クリックが二重発火して効かなかった
- 共有で受け取った半荘のIDが元の記録と同じで、同じ端末に両方あると別のコミュニティの画面が出た（受け取り側でIDに印を付けて解決）
